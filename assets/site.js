document.head.insertAdjacentHTML('beforeend',`<link rel="stylesheet" href="${location.pathname.includes('/characters/')||location.pathname.includes('/content/')?'../':''}assets/v2.css">`);
document.head.insertAdjacentHTML('beforeend',`<link rel="stylesheet" href="${location.pathname.includes('/characters/')||location.pathname.includes('/content/')?'../':''}assets/status.css">`);
const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];
const menu=$('.menu'),nav=$('#nav');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',open);nav.classList.toggle('open',open)});$$('#nav a').forEach(a=>a.addEventListener('click',()=>{menu?.setAttribute('aria-expanded','false');nav?.classList.remove('open')}));
async function archive(){const box=$('#archive-results');if(!box)return;const data=await fetch('assets/content-index.json').then(r=>r.json()),search=$('#archive-search'),filters=$('#archive-filters'),count=$('#archive-count');let active='all';const favs=()=>new Set(JSON.parse(localStorage.getItem('eirakar-favorites')||'[]'));const categories=['all',...new Set(data.map(x=>x.category))];filters.innerHTML=categories.map(x=>`<button data-filter="${x}" class="${x==='all'?'active':''}">${x==='all'?'Todo':x}</button>`).join('');function render(){const q=search.value.toLowerCase().trim(),saved=favs();const shown=data.filter(x=>(active==='all'||x.category===active)&&(!q||`${x.title} ${x.label} ${x.excerpt}`.toLowerCase().includes(q)));count.textContent=`${shown.length} documentos`;box.innerHTML=shown.map(x=>`<article><div><span>${x.category}</span><button class="favorite ${saved.has(x.id)?'active':''}" data-id="${x.id}" aria-label="Guardar ${x.title}">✦</button></div><h2><a href="${x.url}">${x.title}</a></h2><p>${x.excerpt}…</p><a class="read" href="${x.url}">Abrir documento →</a></article>`).join('')||'<p class="empty">No hay documentos que coincidan.</p>';$$('.favorite',box).forEach(b=>b.onclick=()=>{const s=favs();s.has(b.dataset.id)?s.delete(b.dataset.id):s.add(b.dataset.id);localStorage.setItem('eirakar-favorites',JSON.stringify([...s]));render()})}search.oninput=render;$$('button',filters).forEach(b=>b.onclick=()=>{active=b.dataset.filter;$$('button',filters).forEach(x=>x.classList.toggle('active',x===b));render()});$('#clear-favorites').onclick=()=>{localStorage.removeItem('eirakar-favorites');render()};render()}
async function gallery(){const grid=$('#gallery-grid');if(!grid)return;const data=await fetch('assets/gallery-index.json').then(r=>r.json()),dialog=$('#lightbox'),names={canonical:'Canonical',historical:'Historical',wip:'WIP',rejected:'Rejected'};grid.innerHTML=data.map((x,i)=>`<button data-i="${i}" class="status-${x.status}"><span class="media-badge">&lt;${names[x.status]}&gt;</span><img loading="lazy" src="${x.src}" alt="${x.label}"><span class="media-label">${x.label}</span></button>`).join('');$$('button',grid).forEach(b=>b.onclick=()=>{const x=data[+b.dataset.i];$('img',dialog).src=x.src;$('img',dialog).alt=x.label;$('p',dialog).textContent=`<${names[x.status]}> ${x.label}`;dialog.showModal()});$('button',dialog).onclick=()=>dialog.close();dialog.onclick=e=>{if(e.target===dialog)dialog.close()}}
async function cinema(){const grid=$('#video-grid');if(!grid)return;const data=await fetch('assets/video-index.json').then(r=>r.json());grid.innerHTML=data.map((x,i)=>`<article><video controls preload="metadata" poster="assets/images/party-campfire.jpg"><source src="assets/media/videos/${x}" type="video/mp4"></video><span>Secuencia ${String(i).padStart(2,'0')}</span><h2>${x.replace(/^\d+_/,'').replace(/_/g,' ').replace('.mp4','')}</h2></article>`).join('')}
archive();gallery();cinema();
